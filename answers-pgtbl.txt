A1:根据 xv6 book 可知 page 0 包含的是进程的代码段，page 2 包含的是进程的栈；page 1 中包含的是进程的数据段，当进程处于用户模式下，是可以访问 page 1 中内容的。

A2:srcva 和 len 都是 uint64 类型的数据，而 uint64 类型相加采用自然溢出，相当于对 2^64 取模。因此，当 len = 2^64 - 1 时就能满足前两个条件，而不满足第三个条件。